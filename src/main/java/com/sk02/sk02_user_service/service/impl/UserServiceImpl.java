package com.sk02.sk02_user_service.service.impl;

public class UserServiceImpl {

    //addUser (UCCDTO dto)
    // User user = MapDto(dto)
    // CA = new CA()
    // CA.set(dto.pss)
    // CA.setUSer(user)
    // CARep.save(CA)
}
